# Entertainment Recommendation

This project uses K-Nearest Neighbors (KNN) for collaborative filtering to recommend items based on user ratings.

## Requirements
- Python 3.x
- Install dependencies: `pip install -r requirements.txt`

## Usage
Run the model.py script to train and save the model.

## Dataset
Uses synthetic user-item rating data for demonstration.
